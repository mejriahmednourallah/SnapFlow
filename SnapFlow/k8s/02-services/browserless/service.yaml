apiVersion: v1
kind: Service
metadata:
  name: browserless
  namespace: snapflow-prod
  labels:
    app: browserless
spec:
  selector:
    app: browserless
  ports:
  - port: 3000
    targetPort: 3000
    name: http
